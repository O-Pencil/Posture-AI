/** 应用主 Logo（SVG 源文件，Web / favicon 使用） */
export const LOGO_SVG_URI = '/logo/LOGO%20(1).svg';

/** 原生端 bundle 用 PNG（由 LOGO (1).svg 导出，发版前可重新生成） */
export const LOGO_PNG = require('../../public/logo/logo-app.png');
